import { makeStyles } from "tss-react/mui";

export const useStyles = makeStyles()(() => ({
  container: {
    height: "100%",
    width: "80%",
    background: "white",
    margin: "auto",
    padding: 0,
  },
}));
